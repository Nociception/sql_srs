SRS

organize tables in the db, and maybe start a cyber sec work

add an int field `difficulty`

add dus exercises

edit LICENSES.md file for DUS

display subject with st.code instead of st.write

check if AS column aliases affect comparisons (equals)

add feature: hide exercise's theme (which can be an unwanted clue)

add text_area filler field (depends on the selex)

users

stop tab primary function in text_area

docstrings TODO

replace all .df() with pl.Dataframe

scroll to query text_area

handle sql injection

unit tests
